var config = require('../config');
var FbTestUrl = 'https://graph.facebook.com/' + fb.appId +'/test-users?'

var url
https://graph.facebook.com/APP_ID/accounts/test-users?
  installed=true
  &name=FULL_NAME
  &locale=en_US
  &permissions=read_stream
  &method=post
  &access_token=APP_ACCESS_TOKEN

// Creates the fake fb users that we want.
var createFbUsers = 